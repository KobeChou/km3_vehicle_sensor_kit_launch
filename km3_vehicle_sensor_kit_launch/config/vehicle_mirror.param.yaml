/**:
  ros__parameters:
    # 车辆本体裁剪框参数 (用于点云过滤，移除车身点)
    # 根据实际车辆尺寸调整，当前为通用默认值
    min_longitudinal_offset: -2.0
    max_longitudinal_offset: 5.0
    min_lateral_offset: -1.2
    max_lateral_offset: 1.2
    min_height_offset: -0.5
    max_height_offset: 2.5
